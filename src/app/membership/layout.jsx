export const metadata = {
  title: 'Membership | Uniteclub',
  description: 'Discover exclusive events, creator-led workshops, wellness experiences and member-only rewards with one annual membership.'
};

export default function MembershipLayout({ children }) {
  return <>{children}</>;
}
