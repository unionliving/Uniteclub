export const metadata = {
  title: 'The Drop | Uniteclub',
  description: 'Check out the latest upcoming events and curated experiences on our calendar.'
};

export default function CalendarLayout({ children }) {
  return <>{children}</>;
}
